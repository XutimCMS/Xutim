{% extends '@XutimCore/admin/base.html.twig' %}

{% block pageTitle %}
    {{
        block is defined ? block.name : 'new block'|trans({}, 'admin')|capitalize
    }}
{% endblock %}

{% block breadcrumbHeader %}
    <twig:Xutim:Admin:Breadcrumbs>
        <li class="breadcrumb-item">
            <a href="{{ admin_path('admin_block_list') }}">Blocks</a>
        </li>
        <li class="breadcrumb-item">New block</li>
    </twig:Xutim:Admin:Breadcrumbs>
{% endblock %}

{% block pageMenu %}
    {% if block is defined %}
        <twig:Xutim:Admin:Button
            tag="a"
            variant="nav"
            href="{{ admin_path('admin_block_show', {id: block.id}) }}"
        >
            <twig:ux:icon name="ph:magnifying-glass" class="icon mr-1" />
            {{ 'Show'|trans({}, 'admin') }}
        </twig:Xutim:Admin:Button>
    {% endif %}
{% endblock %}

{% block body %}
    <div class="rounded-xl border border-border">
        <div class="p-4">{{ form(form) }}</div>
    </div>
{% endblock %}
