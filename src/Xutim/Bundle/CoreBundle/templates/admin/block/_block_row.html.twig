{% import '@XutimCore/admin/common/badges.html.twig' as badges %}
<tr>
    <td>
        <a
            href="{{ admin_path('admin_block_show', {id: block.id}) }}"
            data-turbo-frame="_top"
            class="block-link"
            title="{{ 'edit'|trans({}, 'admin') }}"
        >
            {{ block.name|truncate(40) }}
        </a>
    </td>
    <td>{{ block.description|truncate(60) }}</td>
    <td>{{ block.layout }}</td>
    <td class="text-center">
        <span class="inline-flex items-center px-2 py-0.5 text-xs font-medium rounded-full bg-surface-raised text-content-secondary">{{ block.articlesItems|length }}</span>
    </td>
    <td class="text-center">
        <span class="inline-flex items-center px-2 py-0.5 text-xs font-medium rounded-full bg-surface-raised text-content-secondary">{{ block.pagesItems|length }}</span>
    </td>
</tr>
