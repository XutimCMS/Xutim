<{{ this.tag }} {{ attributes.defaults({class: this.variantClasses ~ ' cursor-pointer'}) }}
    {% if this.disabled %}disabled aria-disabled="true" class="{{ this.variantClasses }} opacity-50 pointer-events-none"{% endif %}
>
    {% block content %}{% endblock %}
</{{ this.tag }}>
