<!DOCTYPE html>
<html lang="{{ locale }}">
<head>
    <meta charset="UTF-8">
    <title>{{ title }}</title>
</head>
<body>
    <main data-test-page="{{ page.id }}">
        <h1>{{ title }}</h1>
        {% if subTitle %}<h2>{{ subTitle }}</h2>{% endif %}
    </main>
</body>
</html>
