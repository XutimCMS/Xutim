framework:
    messenger:
        default_bus: command.bus
        buses:
            command.bus:
            event.bus:
                default_middleware: allow_no_handlers
                middleware:
                    - Xutim\CoreBundle\Messenger\EventLogMiddleware

        transports:
            async: 'in-memory://'
            sync: 'sync://'
